import { Metadata } from "next"
import { PropsWithChildren } from "react"

export const metadata: Metadata = {
  title: "Получатель и посылка",
  description: "Получатель и посылка",
}

export default ({ children }: PropsWithChildren) => children
